programId: simplified_code_example
name: Simplified Code Example
description: Example program demonstrating simplified Python and bash code execution in steps
tracks:
  - trackId: track1
    name: Python Code Examples
    steps:
      - stepId: python_simple
        name: Simple Python Calculation
        description: Execute a simple Python calculation
        startTrigger:
          type: programStart
        duration:
          type: fixed
          seconds: 10
        script: |
          result = 2 + 2
          print(f'The result is {result}')
      
      - stepId: python_list
        name: Python List Operations
        description: Perform operations on a list
        startTrigger:
          type: afterStep
          stepId: python_simple
        duration:
          type: fixed
          seconds: 15
        script: |
          numbers = [1, 2, 3, 4, 5]
          squared = [n**2 for n in numbers]
          print(f'Original: {numbers}')
          print(f'Squared: {squared}')
      
      - stepId: python_error
        name: Python Error Example
        description: Demonstrate error handling
        startTrigger:
          type: manual
          triggerName: Run Error Example
        duration:
          type: fixed
          seconds: 5
        script: |
          # This will cause a division by zero error
          result = 10 / 0
  
  - trackId: track2
    name: Bash Script Examples
    steps:
      - stepId: bash_echo
        name: Simple Echo Command
        description: Execute a simple echo command
        startTrigger:
          type: programStart
        duration:
          type: fixed
          seconds: 10
        shell: echo 'Hello from bash!'
      
      - stepId: bash_ls
        name: List Directory Contents
        description: List the contents of the current directory
        startTrigger:
          type: afterStep
          stepId: bash_echo
        duration:
          type: fixed
          seconds: 15
        shell: ls -la
      
      - stepId: bash_error
        name: Bash Error Example
        description: Demonstrate error handling in bash commands
        startTrigger:
          type: manual
          triggerName: Run Bash Error
        duration:
          type: fixed
          seconds: 5
        shell: cat nonexistent_file.txt

resourceConstraints: [] 